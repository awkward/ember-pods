Controller = Ember.Controller.extend
  init: ->
    # Initialized default controller.
    # You could also extend ArrayController or ObjectController
    # depending on the data returned from the route model hook

`export default Controller`