moduleForModel '<%= modelName %>', '<%= modelName %> model',
  needs: []
  setup: ->
  teardown: ->

test "testing the <%= modelName %> model", ->
  # Create a new model
  model = @subject({ key: 'value' })
  ok false, 'unimplemented test for <%= modelName %> model'
