moduleFor 'controller:<%= podName %>', '<%= podName %> controller',
  needs: []
  setup: ->
  teardown: ->

test "testing the <%= podName %> controller", ->

  ok false, 'unimplemented test for <%= podName %> controller'
