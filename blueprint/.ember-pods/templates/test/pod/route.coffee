moduleFor 'route:<%= podName %>', '<%= podName %> route',
  needs: []
  setup: ->
  teardown: ->

test "testing the <%= podName %> route", ->

  ok false, 'unimplemented test for <%= podName %> route'
